/*
  Warnings:

  - You are about to drop the column `id_user_config` on the `tbl_users` table. All the data in the column will be lost.

*/
-- AlterTable
ALTER TABLE "tbl_users" DROP COLUMN "id_user_config";
